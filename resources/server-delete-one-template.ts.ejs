<% if (entity.api && entity.api.booleans.hasDeleteOne) { -%>
// entity imports
import <%= `${utils.toTitleCase(entity.name)}Handler` %> from '<%- entity.apiNext.packages.routeHandler %>';

export const POST = <%= `${utils.toTitleCase(entity.name)}Handler` %>.deleteOne;
<% } -%>
