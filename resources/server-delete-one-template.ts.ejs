<% if (entity.api && entity.api.booleans.hasDeleteOne) { -%>
import * as <%= entity.api.strings.actionClass %> from '@/backend/entities/generated/<%= utils.toKababCase(entity.name) %>-action';
import { handleDeleteOne } from 'airent-api';
import { <%= entity.api.strings.getOneParams %> } from '<%= entity.api.request.import %>';
<%- config.authenticatorImport %>

export const POST = handleDeleteOne({
  paramsZod: <%= entity.api.strings.getOneParams %>,
  apiExecutor: <%= entity.api.strings.actionClass %>.<%= entity.api.strings.deleteOneAction %>,
  authenticator,
  options: {
  <%_ if ((entity.api.internalMethods ?? []).includes('deleteOne')) { -%>
    requireInternal: true,
  <%_ } -%>
  <%_ if ((entity.api.adminMethods ?? []).includes('deleteOne')) { -%>
    requireAdmin: true,
  <%_ } -%>
  <%_ if ((entity.api.publicMethods ?? []).includes('deleteOne')) { -%>
    requireLogin: false,
  <%_ } -%>
  },
});
<% } -%>
