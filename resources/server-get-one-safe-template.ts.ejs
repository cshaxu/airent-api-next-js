<% if (entity.api && entity.api.booleans.hasGetOneSafe) { -%>
// entity imports
import <%= `${utils.toTitleCase(entity.name)}Handler` %> from '<%- entity.apiNext.packages.routeHandler %>';

export const POST = <%= `${utils.toTitleCase(entity.name)}Handler` %>.getOneSafe;
<% } -%>
