<% if (entity.api && entity.api.booleans.hasUpdateOne) { -%>
import { <%= entity.api.strings.updateOneHandler %> } from '<%= config.nextHandlersPath %>/<%= utils.toKababCase(entity.name) %>-handlers';

export const POST = <%= entity.api.strings.updateOneHandler %>;
<% } -%>
