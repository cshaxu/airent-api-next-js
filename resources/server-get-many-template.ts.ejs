<% if (entity.api && entity._booleans.api.hasGetMany) { -%>
// entity imports
import <%= `${utils.toTitleCase(entity.name)}Handler` %> from '<%- entity._packages.apiNext.routeToHandlerFull %>';

export const POST = <%= `${utils.toTitleCase(entity.name)}Handler` %>.getMany;
<% } -%>
