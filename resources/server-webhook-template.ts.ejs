// airent imports
import { handle } from '@airent/api';

// config imports
import { handlerConfig } from '<%- handlerConfigPackage %>';

// function imports
import { parser, validator, executor } from '<%- entry.parsed.sourcePackage %>';

export const POST = handle({ 
  ...handlerConfig,
  parser,
  validator,
  executor,
});
